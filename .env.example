# Configuration
MAX_CONTENT_LENGTH=10485760  # 10MB in bytes
REQUEST_TIMEOUT=30  # seconds

# Trafilatura Settings
EXTRACTION_TIMEOUT=30
MIN_EXTRACTED_SIZE=250  # minimum number of characters for extracted content
EXTRACTION_INTENSITY=2  # 1-3, higher means more aggressive cleaning (default: 1)

# Optional: Proxy Configuration (if needed)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=https://proxy.example.com:8080

# Optional: Rate Limiting
# REQUESTS_PER_MINUTE=60
# MAX_CONCURRENT_REQUESTS=10

# Optional: Cache Configuration
# CACHE_ENABLED=true
# CACHE_DURATION=3600  # seconds